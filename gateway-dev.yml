zuul:
  routes:
    myProduct:
      path: /myProduct2/**
      serviceId: product
      #sensitiveHeaders:
#  ignored-patterns:
#    - /myProduct/product/listAllUp
management:
  endpoints:
    web:
      exposure:
        include: "*"
