zuul:
  routes:
    myProduct:
      path: /myProduct/**
      serviceId: product
#  ignored-patterns:
#    - /myProduct/product/listAllUp
  sensitive-headers:
management:
  endpoints:
    web:
      exposure:
        include: "*"
