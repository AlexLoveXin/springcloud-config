zuul:
  routes:
    myProduct2:
      path: /myProduct2/**
      serviceId: product
      #sensitiveHeaders:
#  ignored-patterns:
#    - /myProduct/product/listAllUp
management:
  endpoints:
    web:
      exposure:
        include: "*"
