zuul:
  routes:
    myProduct3:
      path: /myProduct3/**
      serviceId: product
      #sensitiveHeaders:
#  ignored-patterns:
#    - /myProduct/product/listAllUp
management:
  endpoints:
    web:
      exposure:
        include: "*"
