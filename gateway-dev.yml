zuul:
  routes:
    myProduct:
      path: /myProduct/**
      serviceId: product
      #sensitiveHeaders:
#  ignored-patterns:
#    - /myProduct/product/listAllUp
management:
  endpoints:
    web:
      exposure:
        include: "*"
